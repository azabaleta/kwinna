Use context.md and contracts.

TASK:
Create MSW mocks in apps/web.

ENDPOINTS:

GET /products
GET /products/:id
GET /stock
GET /stock/:productId
POST /stock/in
POST /sales

REQUIREMENTS:

- Use realistic data
- Keep stock consistent
- Use contracts

OUTPUT:
- handlers
- setup

CONSTRAINTS:
- no fake structure outside contracts